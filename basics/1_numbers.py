# Dealing with numbers in python.
number = -8
decimal = 02.08

# Absolute Value:
print(abs(number))

# Rounding Off:
print(round(decimal))
print(round(decimal, 1))        # Rounding the decimal off to 1 decimal place.